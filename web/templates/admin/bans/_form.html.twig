{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.type) }}
    {{ form_row(form.steam) }}
    {{ form_row(form.ip) }}
    {{ form_row(form.reason) }}
    {{ form_row(form.length) }}
    {{ form_row(form.submit, {'attr': {'class': 'btn-primary'}}) }}
{{ form_end(form) }}
