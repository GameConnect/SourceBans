{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.folder) }}
    {{ form_row(form.icon) }}
    {{ form_row(form.submit, {'attr': {'class': 'btn-primary'}}) }}
{{ form_end(form) }}
