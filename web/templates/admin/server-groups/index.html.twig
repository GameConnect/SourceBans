{% extends 'base.html.twig' %}

{% block title %}Server groups « {{ parent() }}{% endblock %}

{% block body %}
    <main class="page">
        <div class="row">
            <div class="col">
                <h2>
                    Server groups
                    <a href="{{ path('admin_server_groups_add') }}" class="btn btn-secondary float-right">Add group</a>
                </h2>
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th width="5%">&nbsp;</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for serverGroup in serverGroups %}
                        <tr>
                            <td>{{ serverGroup.name }}</td>
                            <td>
                                <a href="{{ path('admin_server_groups_edit', {'id': serverGroup.id}) }}">Edit</a>
                                <a href="{{ path('admin_server_groups_delete', {'id': serverGroup.id}) }}">Delete</a>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </main>
{% endblock %}
